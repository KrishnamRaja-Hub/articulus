node_modules
dist
.DS_Store
tsconfig.tsbuildinfo
*.log
# data pipeline scratch space (staging, reports)
.pipeline
# pipeline lock files and crashed-publish leftovers (removed by the next run)
.data.lock
.data-next-*
.data-prev-*
# local agent worktrees
.claude/worktrees/
